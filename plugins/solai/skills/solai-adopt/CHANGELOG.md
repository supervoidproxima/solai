# CHANGELOG - start-adopt

| # | Date | Source | Section | Change | Reason | Status |
|---|---|---|---|---|---|---|
| 1 | 2026-08-27 | initial build | all | Nine probes, inference of card classes from disk, conformance table, ordered migration plan, mandatory not-checked section. | A vault that cannot be read cannot be adopted. | done |
| 2 | 2026-08-27 | run against Fangorn | P8 | A generated-file claim must now be self-referential: shouted in the frontmatter, made in the opening lines of the body, or phrased as an instruction not to edit. Previously any occurrence of the word "generated" in the first 4 KB counted. | Fangorn returned 41 findings, of which 0 were real. Its charter states "the generator reads the field, not the prose" and was accused of being generated. A probe wrong 40 times in 41 teaches its reader to skip the row. | done |
| 3 | 2026-08-27 | run against Fangorn | P4 | A plural beside its singular is reported as one concept under two names only when both hold the same shape of value. Differing shapes go to a separate advisory table. | `sources: [a, b]` and `source: "[[x]]"` are a collection and a pointer. Three of Fangorn's four reported pairs were legitimate, and NIS's own property dictionary explicitly permits natural plurals for collections. | done |
| 4 | 2026-08-27 | run against Fangorn | P5 | The tag vocabulary is located by filename containing tag, vocab or properties anywhere under `_system/`, and the report names the files it read. Previously three exact filenames were tried. | Fangorn's list is `_system/Tag vocabulary.md`. The report said "no declared list found" about a vault that has one, and then proposed writing the file that already existed. | done |
| 5 | 2026-08-27 | run against Fangorn | P3 | `_`-prefixed files are excluded from the denominator when deciding whether a folder holds a card class, and only `_system/os/` is excluded outright rather than all of `_system/`. | `wellbeing/prescriptions/` holds RX-001 to RX-003 beside one `_template.md`: three of four is 75%, under the 80% threshold, so a real class went undetected. | done |
| 6 | 2026-08-27 | run against Fangorn | report step 5 | Where a tag list exists but most tags in use are absent from it, the migration proposes reconciliation rather than dropping the step. | 162 of Fangorn's 198 tags are undeclared. A closed list that most tags ignore is not closed, and silence about it reads as conformance. | done |
