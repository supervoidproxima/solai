# CHANGELOG - solai

| # | Date | Source | Section | Change | Reason | Status |
|---|---|---|---|---|---|---|
| 1 | 2026-08-27 | initial build | all | Package created: doctrine, conventions, TOML class declarations, five emitters, plan/apply/rollback engine, tier-aware validator, read-only retrofit probe, minimal and project archetypes. | Two mature vaults each held half of a good answer, and neither could reproduce itself. | done |
| 2 | 2026-08-27 | deferral list, plan | archetypes/personal, archetypes/position, README | Built the two deferred archetypes. `personal` (light, five classes: lab, prescription, visit, account, liability) takes its class set from what the read-only probe actually found hand-built in Fangorn, not from a guess. `position` (standard, three classes: claim, experiment, application) puts every class on the loop claim to experiment to application, so nothing in it can be worked on without moving the search. Version 0.2.0. | The plan deferred both and predicted `lib/` would not need to change. It did not: both are declaration-only, and both pass the twice-apply NOOP test, the validator, the stamp check and the link check on a fresh vault. | done |
| 3 | 2026-08-27 | user request | archetypes/role, README, tiers.md | Archetype `position` renamed to `role`. Directory, `archetype` key and title (now "Role search"); no class, field or engine change. | Shorter, and it names the thing being searched for rather than the abstraction around it. Row 2 above keeps the old name because it records what was true when it was written. | done |
| 4 | 2026-08-27 | user request | skills/place, skills/place-scaffold, skills/place-adopt, manifests, fragments, README, conventions, doctrine, lib docstrings | Command renamed `/start` → `/place`. Three skill folders and their frontmatter `name`, both trigger phrases, every quoted command in prose, the `[[pipeline]]` rows of three manifests, and the runs folder `_system/runs/start/` → `_system/runs/place/`. The package, the plugin, the marketplace, `~/.claude/start/`, the region marker prefix `<!-- start:begin -->` and `GENERATED_BY` are unchanged: they name the writer, not the command. | `start` is an imperative verb, so `/start check` was verb-verb and `/start adopt <path>` was ungrammatical; bare `/start` meaning status was the clearest tell. A command whose name means the wrong thing is this package failing its own D6, and the object-noun convention matches the ~90 skills already in this config. Renamed before any vault carries a bond file, which is the cheapest moment it will ever be. | done |
| 5 | 2026-08-27 | user request | everything | Package renamed `start` → `place`, completing CHG 4. Directory `~/.claude/start/` → `~/.claude/place/` and `plugins/start/` → `plugins/place/`; marketplace and plugin names; the `.gitignore` allowlist line; the region marker prefix `<!-- start:begin -->` → `<!-- place:begin -->` with its reader in `stamp_check.py`; the bond keys `start-package`/`start-archetype`/`start-tier` → `place-*` with their reader in `gen_dashboard.py`; `GENERATED_BY`; the temp-file suffix; and every prose reference. Version 0.3.0. Install is now `/plugin marketplace add ~/.claude/place` then `/plugin install place@place`. | CHG 4 left the package called `start` and everything typed called `place`, which is the half-renamed state that is worse than either end. Breaking by design: the marker prefix and the bond keys change, so any vault built by 0.2.0 would read FOREIGN. None exist, which is why it was done now and cannot be done later. | done |
| 6 | 2026-08-27 | vocabulary review | lib/decl.py, lib/emit/claudemd.py, both label files, three manifests, state fragment, doctrine D12 | Manifest key `[[pipeline]]` → `[[loop]]`, with the region id, the `Archetype.loop` accessor, the emitter function and the label. Headings now read "The loop" and «Порядок работы». `decl.py` gains a guard that refuses a manifest still carrying `[[pipeline]]` by name. Version 0.4.0. | `pipeline` was doing two jobs: the ordered way of working in a place, and a concurrency shape inside a workflow. One word, two ideas, which is the exact drift D6 forbids. `pipeline` is now reserved for the concurrency sense only. «Конвейер» was also replaced because «Цикл» and «Поток» are both already taken in an existing vault, and a third near-synonym would be a false friend. Breaking: the region id changed, so a file built by 0.3.0 reads FOREIGN on that region. | done |
| 7 | 2026-08-27 | handoff step 2 | tests/ | Test suite re-created and committed: `tests/run_tests.py` runs 84 assertions (45 primitives - `fm` 14, `stamp` 11, `regions` 13, `fsplan` 7 - and 39 on the declaration loader), each with a stable id, on a stdlib harness that counts assertions rather than test functions and refuses a run whose count does not match the declared expectation. `tests/verify_engine.py` turns the end-to-end verification from prose into a command: four archetypes scaffolded and applied, second plan required to be entirely NOOP, then the three checkers, with `personal` built in Russian. No `lib/` or archetype file changed, so the version stays 0.4.0. | The 84 assertions passed on 2026-08-27 and were lost with a session scratchpad, leaving the engine with no committed evidence of its own state at exactly the point where steps 3 to 6 begin changing it. Two assertions were found weak by mutation-testing the suite against 18 single-edit breakages of `lib/` and were strengthened: `ST-02` asserted CRLF-blindness with a trailing CR, which `rstrip` covers unaided, and nothing covered `insert` delegating to `replace`, which is the twice-apply guarantee at its smallest. The verification also corrects a unit ambiguity: the expected NOOP counts are 5 / 24 / 24 / 20 plan rows, not 6 / 25 / 25 / 21; the difference is `dashboard.html`, declared as an artefact but never planned because it reflects the vault after the apply. A version bump was deliberately avoided: it would make every generated file in every vault read STALE to record a change no vault can see. | done |
| 8 | 2026-08-27 | handoff steps 3 and 4 | lib/decl.py, lib/emit/agent.py, lib/engine.py, common/agents/, both label files, four manifests, tests/ | **The agent layer.** An `agents` key on the archetype manifest and a seventh emitter beside the other six. `decl.py` gains `Agent`, `Return`, `Eval`, `_validate_agent` and `load_agent`; the loading loop now runs over three declaration kinds instead of two, which is the whole of the change needed to carry agents through the archetype-then-common fallback. `emit/agent.py` projects four regions (frontmatter, job, returns, evals) into `.claude/agents/<name>.md`, and the declaration is compiled into `_system/os/agents/`. Four agents written: `scout`, `refuter`, `extractor`, `editor`, each with a declared job, a return schema and evals carrying a `fails_when`. All four are read-only and mint nothing. Selection is per archetype over one shared roster: minimal none, personal two, project and role four. Agents count towards the anti-avoidance skills cap. 39 new assertions (123 total) and the version is 0.5.0. | Two open questions were settled before any code was written. Agents are declared ONCE in `common/agents/` and SELECTED per archetype, because four generic agents copied into four archetypes is the copy-holding defect this package exists to remove - and the loader already had the fallback, so the answer cost nothing. Agents are PROJECTED into a place rather than read from the package, following the two decisions already recorded: a place does not read the package at run time, and the card skills chose projection. Read-only is structural, not modest: an agent that wrote cards would become a second minter of a prefix that already has one, so `writes = false` is refused if it asks for a writing tool. `job` under 40 characters, a missing return schema, and an eval with no `fails_when` are all refusals, the last because an eval that cannot fail is a description and a roster of descriptions is exactly the machinery D1 forbids. Unknown keys are refused everywhere after a stray `whenever =` was typed into `scout.toml` and the loader accepted it in silence. | done |
| 9 | 2026-08-27 | found while verifying row 8 | runtime/stamp_check.py, tests/verify_engine.py | **Two defects in the stamp checker, both pre-existing.** (1) `.claude` sat in `SKIP_DIRS` beside `.git` and `node_modules`, so the checker had never opened a single `SKILL.md` or agent file. It now descends into `.claude/skills/` and `.claude/agents/` and nothing else under `.claude/`. (2) Volatile-line stripping was applied to every region of every file, but the engine strips volatile lines for `.base` files only. One of the patterns, an indented `key: <digits>`, matches `contract_version: 1` - a line the engine writes into every SKILL.md and agent frontmatter at standard tier - so the reader hashed different bytes than the writer and a freshly generated file read HAND-EDITED. Stripping is now confined to `.base`, matching the writer. Coverage went from 14 stamped items to between 45 and 55 per archetype, all clean. `verify_engine.py` gains a probe: after everything reads clean it edits one generated region and requires the checker to name it. | Defect 2 was invisible because of defect 1: the only files carrying `contract_version: 1` were the ones inside the skipped directory. Fixing the skip surfaced it within one run, which is the argument for the fix by itself. The probe exists because "all stamped files match" is what a checker that opened no files also reports, and for the whole life of the package that is precisely what it was reporting about `.claude/`. A checker nobody tests against a real edit is a checker that is trusted rather than working. | done |
| 10 | 2026-08-27 | handoff step 5 | lib/decl.py, lib/emit/workflow.py, lib/engine.py, common/workflows/, four manifests, tests/ | **The workflow layer.** A `workflows` key on the manifest and an eighth emitter. `decl.py` gains `Workflow`, `Phase`, `_validate_workflow` and `load_workflow`; the loading loop now runs over four declaration kinds. `emit/workflow.py` projects two regions (`meta`, `script`) into `.claude/workflows/<name>.js`, with a JSON Schema per agent call generated from that agent's own `[returns]` block. Three workflows written: `review` (scout then refuter, both pipelines), `intake` (extractor then a justified barrier), `polish` (scout then editor). Selection per archetype: minimal none, personal none, project three, role two. Two committed checkers: `check_workflow_js.py` validates each projection as real JavaScript with an evaluable meta literal, and `check_workflow_resume.py` replays each script twice and kills one mid-run. 39 new assertions (162 total). Version 0.6.0. | A barrier must justify itself in prose next to the stage that pays for it, because a barrier makes every branch wait for the slowest and the default is a pipeline; a workflow of nothing but barriers is refused outright. A single-phase workflow is refused as an agent call with ceremony around it. The `carry` key was added after reading the first projection: the data flow between phases had been implied by prose, the chain was named correctly for exactly two phases and silently wrong at three, and a non-first stage interpolated result OBJECTS into its prompt - so the second agent would have been asked to work on `[object Object]` and would have done it. Resume is why the projection is a script rather than a runbook, and what resume depends on is a stable call sequence, so the emitter refuses to write wall-clock or randomness and the replay checker proves the sequence rather than assuming it. | done |
| 11 | 2026-08-27 | found while projecting step 5 | lib/regions.py, runtime/stamp_check.py | **The `// ` marker prefix had no reader.** The shared region regex tolerated a leading `#` (for YAML) and nothing else, so the workflow emitter wrote stamp markers that neither the checker nor the ENGINE'S OWN MERGE could find. Both readers now accept `#` or `//`: the one in `lib/regions.py` and the copy inside `runtime/stamp_check.py`. `stamp_check` also filtered on `.md` and `.base`, so it now reads `.js` and descends into `.claude/workflows/` as well. | The handoff names this exact trap - a marker prefix and its reader must move in the same commit - and it still caught me, because the prefix was new rather than renamed. The failure was not subtle in effect: every region read absent, so the second apply would have re-inserted markers forever and the twice-apply guarantee would have died on the newest artefact type. The extension filter is the same blindness that hid the SKILL.md problem until 0.5.0, fixed here before it could become the same surprise a second time. | done |
| 12 | 2026-08-27 | handoff step 6 | skills/place/author.py, skills/place/authoring.md, skills/place/SKILL.md, skills/place/refusals.md, runtime/check_agents.py, four manifests, common/agents/reconciler.toml, tests/ | **The authoring verbs.** `/place agent <name>` and `/place workflow <name>` write a declaration and wire it into the archetypes that take it, through `author.py`: plan by default, `--apply` to write, backed by the same `fsplan` plan/apply/rollback contract as everything else. `/place check --agents` runs the new `check_agents.py`, which ships into every archetype that carries the other checkers: nine structural assertions over a place's own compiled declarations, then the eval roster. Refusals: a name already taken by an agent, a workflow or a card class; a job repeating 60% or more of the significant words of an existing one; no return field; no eval; and any declaration the loader would reject. `SKILL.md` gained the verbs and was trimmed back to exactly its own 200-line cap, with the `--fix` closed set moved to `refusals.md`. The gate was met by authoring a fifth agent, `reconciler`, end to end through the verb with no projection hand-edited. 16 new assertions (178 total). Version 0.7.0. | Adding four agents and three workflows by hand is what these verbs cost, and that cost is the argument for them. Two design choices carry the weight. The substantive content is required on the command line rather than scaffolded as placeholders, and the rendered declaration is validated in a temp file before anything is planned, because a placeholder declaration either breaks the loader for whoever runs the engine next or passes it while meaning nothing. And `check --agents` reports its evals as OUTSTANDING rather than as a score: an eval judges an agent's answer, so executing one takes a model call that a deterministic script cannot make, and printing a number it did not measure is the one dishonest move the verb makes available. `reconciler` was chosen because the lesson it encodes is already recorded on this machine - judge a received document against the question the request asked, not against the request's title - and it declares `induces: title-matching` for the failure that lesson is about. | done |
| 13 | 2026-08-27 | found by the new tests | skills/place/author.py | **Two defects in the authoring script, both caught before it shipped.** (1) `kind` meant two things: the subcommand (`agent` or `workflow`) and an agent's declared RETURN kind. The generic argument parser wrote `kind = "agent"` into the `[returns]` block; the subcommand is now `verb` and the return kind is `--returns-kind`. (2) The job-overlap check used Jaccard similarity, which divides by the union, so a short job that was entirely a SUBSET of a long existing one scored low - and a subset is the clearest duplicate there is. Restating scout's first sentence scored 0.43 and passed. It now uses containment, dividing by the smaller set, so a subset scores 1.0. | Defect 1 is the trap the handoff names about a name that means two things, occurring inside the tool built to reduce hand-editing, and it was caught by the validate-before-write rule rather than by review: the loader refused `kind = "agent"` and nothing reached disk. Defect 2 was caught by the assertion written to cover it, which is the argument for writing the test that restates an existing job rather than assuming a similarity metric does what its name suggests. | done |
| 14 | 2026-08-28 | user request | skills/place-scaffold/serve.py, skills/place-scaffold/ui.html, skills/place-scaffold/SKILL.md, tests/test_ui.py, tests/run_tests.py, lib/__init__.py, plugin.json | **The setup surface.** A stdlib HTTP server on the loopback interface, keyed per process, serving one page that counts the path, renders the questions the chosen archetype declares, shows the plan table and then creates. It builds nothing itself: every plan and apply is a `scaffold.py` subprocess whose output is relayed verbatim, refusals included. `--plan` before `--apply` is enforced by a fingerprint of the answer set rather than by discipline - editing a field voids it, and an apply spends it. Inert answers (`mode`, `classes`) are rendered as statements with the reason, never as controls. 23 new assertions (201 total), four of them mutation-tested. Version 0.8.0. | A page cannot be told to plan first, and the operator drives this one with a mouse: the rule had to move from prose into a token or it would have been the first thing lost. A served page rather than a file or a published artefact because only a process on this machine can create a folder, and saying so plainly is cheaper than a surface that looks like it works offline. The version bump is honest about what changed and costs nothing today: no place carries a bond yet, so nothing reads STALE. This surface counts on the document side of its own D16 ratio, and the place it exists to build has still not been built. | done |
| 15 | 2026-08-28 | user request | archetypes/{minimal,personal,project,role}/manifest.toml, skills/place-scaffold/{serve.py,ui.html,SKILL.md}, tests/test_ui.py, lib/__init__.py, plugin.json | **The surface is named Solai, and the kinds are renamed.** Titles are now bare nouns - Minimal, Personal, Project, Role - and `project` no longer calls itself a consulting engagement. The surface offers `role` only; the other three are rendered disabled with the reason, and the API refuses them rather than building what the page says it will not. Steps reordered to Kind, Name, Place, Answers, Plan: the name is asked first and the folder path is suggested from it under a `--vaults` base, and stops being suggested once edited. Redesigned around a sticky step rail that reads state rather than clicks, count tiles, plan pills parsed from the engine output, and a completion panel. 4 new assertions (208 total). Version 0.9.0. | `Role` had meant a job search; the operator redefined it as the role one has taken up, seeded with the JD, the regulations and the plans. The title changed in this row and the CONTENTS did not, which is recorded here as the open defect rather than left implied: the archetype still declares claim/experiment/application, a `search` folder and a market-sweeping scout, and it would build a job hunt for anyone who pressed create. The next row must either repurpose `role` and keep the search shape as its own kind, or revert the title. Naming the page separately from the package is the same distinction the rename in row 4 drew between a command and a writer: `place` is the engine, Solai is what a person opens, and «just so» is what the engine spends its whole run establishing. | done |
| 16 | 2026-08-28 | user request, closing the defect recorded in row 15 | archetypes/role/ (rewritten), archetypes/search/ (new), tests/{test_decl.py,verify_engine.py,README.md}, README.md, skills/place/tiers.md, lib/__init__.py, plugin.json | **`role` now means the role you hold, and the job search becomes its own kind.** The archetype declares `obligation` (one thing the role owes, traced to the clause or the instruction that imposes it), `deliverable` (one thing that leaves, to a named recipient, opened at the promise rather than at the sending) and `decision` (what was chosen, under what authority, displacing what, at what cost to reverse). Folders: `charter`, `plans`, `obligations`, `deliverables`, `decisions`, `sources`, `people`. The partition binds `area` rather than `track`. The previous contents - claim / experiment / application, the market tracks, the `search` and `profile` folders - move unchanged to a fifth archetype, `search`, which is what `Fangorn/career/` already is. Agents and workflows are unchanged on both. Five existing assertions widened to five archetypes rather than four, and the engine gate gains a `search` row: 208/208, NOOP 5 / 41 / 29 / 33 / 33. Version 0.10.0. | A promise made to a person is NOT a fourth class. A promise and the thing promised are one object at two moments, so `deliverable` opens in `promised` and carries `due`, `sent-on` and a `dropped` terminal; two cards for one object would be the pair nobody compares that D3 exists to forbid. `scout` was kept against the proposal in the handoff note, which listed extractor / editor / refuter: the scout is sent at a source rather than at a market, a role is constituted by documents it did not write, and dropping it would have cost the `polish` workflow, whose first phase calls it. `charter/` rather than `mandate/` because the operator's lexicon bans the word and an English folder name still pulls the calque into the Russian prose written beside it. `decision` is gated with a required `authority` and `reversal-cost` and a required `## What it displaces` because it is the class most likely to decay into a diary. Splitting rather than deleting the search shape follows D7: a retirement names the fate of what it affects, and this one is migrate, not orphan. | done |
| 17 | 2026-08-28 | user correction | archetypes/search/ (deleted), tests/{test_decl.py,verify_engine.py,README.md}, README.md, skills/solai/tiers.md | **`search` withdrawn.** The archetype added in row 16 is deleted and the four widened assertions are back at four archetypes. The job-search declarations - claim, experiment, application, the market tracks, the `search` and `profile` folders - no longer exist in the package. | It was never asked for. Row 16 carried it in from the previous session's handoff proposal and the operator had not accepted that half; keeping the search shape alive was a judgement made on their behalf, which is the thing D17 refuses. Deleted rather than parked: an archetype nobody chose, sitting on disk, is a claim the package makes about itself. The repurposing of `role` in row 16 stands unchanged. | done |
| 18 | 2026-08-28 | user request | every path and identifier in the package | **The engine is renamed `place` to `solai`.** Directories: `~/.claude/place/` to `~/.claude/solai/`, `plugins/place/` to `plugins/solai/`, and the three skills to `solai`, `solai-scaffold`, `solai-adopt`. Command `/place` to `/solai` in every verb. Region markers `place:begin` and `place:end` to `solai:begin` and `solai:end`, with the regex in `lib/regions.py` and the dashboard stamp moved in the same pass. Bond keys `place-package`, `place-archetype`, `place-tier` to `solai-*`, and `gen_dashboard.py` reads the new keys. Both gates green after the move: 208/208 and NOOP 5 / 41 / 29 / 33. Version 0.11.0. | The split recorded in row 15 - `place` the engine, Solai the page - is dropped: it cost a sentence of explanation in every file that mentioned either and bought nothing a reader wanted. The common noun survives untouched: a place is still what the engine builds, and a global word swap would have rewritten several hundred sentences that mean a vault. Renaming the markers was affordable exactly once, today, because no real place exists yet: every generated file carries them, and a marker change after a place is built is a migration rather than a rename. | done |
| 19 | 2026-08-28 | user request | skills/solai-scaffold/ui.html, archetypes/role/manifest.toml, tests/test_ui.py | **Setup copy cut back, and the rail footer removed.** The block at the foot of the rail - the Kazakh gloss, the engine version, the package path and the line about the terminal - is gone with its styles. Every step lede and every field hint is one short sentence: the assertion kept, the explanation of the assertion dropped. Rail reads Kind / Name / Folder / Answers / Plan. The partition field is now labelled from the archetype's own `binds` key, so `role` asks for Areas and an archetype binding `track` asks for Tracks. No version bump: nothing an emitter reads changed. | The footer explained the tool to a person who had already opened it, which is the machinery D16 counts on the document side. The partition label had been hardcoded `Tracks` since the archetype that introduced it was a job search; leaving it after row 16 would have asked a role-holder about tracks it does not have, which is the surface asserting a vocabulary the declaration does not. | done |
| 20 | 2026-08-28 | user request | skills/solai-scaffold/ui.html, archetypes/role/manifest.toml | **Second copy pass: spoken and shorter.** Headings are questions - What kind? / Call it what? / Where does it go? / A few answers / Look, then build. Every lede is one plain sentence. `Remit` is now labelled **What it is for**; `Output language` is **Language**; `First artefact, and by when` is **First thing you will ship, and when**. The answer key sent to the engine is still `remit`, so nothing downstream moved. No version bump. | The first pass shortened the sentences but kept the register of a specification, and the person opening this page is setting up a folder, not reading a contract. `Remit` was the clearest example: a word nobody says out loud, sitting on the one field that has to be answered in the operator's own words. | done |
| 21 | 2026-08-28 | user request | skills/solai-scaffold/{ui.html,serve.py}, tests/test_ui.py | **`kind` is gone from the surface, inside and out, and the copy is rewritten as headlines.** The word is replaced by `type` everywhere it was visible and everywhere it was not: `state.kind` to `state.type`, `#kinds` to `#types`, `#kind-pick` to `#type-pick`, the CSS class `.kind` to `.type-card`, and the comments in `serve.py` and `tests/test_ui.py` with them. Steps now read Choose a type. / Give it a name. / Choose where it lives. / Add a few details. / Review, then create., each with one line under it. Field labels: **Purpose** (was Remit, then What it is for), **Language**, **First deliverable**, **Tier**. Buttons: Check, Review, Create folder, Open dashboard, Copy path. On success: «Your role place is ready.» 208/208, the page's script parses, no duplicate ids, no dangling selectors. No version bump. | A label the reader has to learn before answering the first question is a cost the surface charges for nothing; `type` needs no gloss. Renaming the internals in the same pass is the cheaper half of the job: an identifier that disagrees with the label is how the next edit reintroduces the word. | done |
| 22 | 2026-08-28 | defects seen in a plan run | skills/solai-scaffold/scaffold.py, lib/fsplan.py, tests/test_primitives.py | **Two defects fixed.** The plan header printed `place 0.11.0` and the atomic write used a `.place-tmp` sibling - two literals the rename in row 18 did not reach, now `solai`. `Plan.mkdir` returns the existing row instead of adding a second one for a path already planned, so `obligations`, `deliverables` and `decisions` - declared once as folders and once as class folders - appear once each. Role's first plan drops from 17 MKDIR rows to 14. One assertion added, FS-08, and `EXPECTED` raised to 46 in its module: 209/209, NOOP unchanged at 5 / 41 / 29 / 33. | A duplicate row is not cosmetic. The plan is the thing a person is asked to approve before anything is written, and a table that counts an action the filesystem performs once is a table that cannot be checked against what happened. The dedupe lives in `fsplan` rather than in the engine loop that produced it: the invariant is about directories, not about who asked. | done |
| 23 | 2026-08-28 | user request | lib/{engine.py,fsplan.py,decl.py}, skills/solai-scaffold/{scaffold.py,serve.py,ui.html,SKILL.md}, tests/{test_primitives.py,test_ui.py} | **Materials at setup.** `--materials <file or folder>`, repeatable, on the engine, and a fourth step on the page between Location and Details. Every file found becomes a COPY row in the same plan, landing in the folder the archetype declares for unprocessed capture - a new manifest key `materials`, `_inbox` by default. `engine.collect_materials` walks a folder, skips dotfiles, and refuses by name: a path that is not there, two sources sharing one basename, a path inside the place itself. `fsplan.bytes_sha` hashes bytes, so a PDF is NOOP on the second plan rather than copied again. The page adds `/api/materials`, which counts and relays refusals without opening a file, and the materials path joins the plan fingerprint, so changing it voids an approved plan. The completion panel names the count waiting in the inbox. 7 assertions added (MAT-01..05, UI-31, UI-32), EXPECTED raised in both modules: 216/216, NOOP unchanged at 5 / 41 / 29 / 33. Version 0.12.0. | The question was whether documents should shape the setup or be handled after it. They are copied at setup and read after it, because the two halves have different natures: moving bytes is mechanical and belongs in a plan a person can approve line by line, while deciding that one document constitutes the role and another is background is judgement, and the surface is a stdlib process with no session and no agent in it. Landing them in `_inbox` rather than in `charter` follows from the same line: `_inbox` means unprocessed, and filing them anywhere more specific would be the classification the engine must not perform. | done |
| 24 | 2026-08-28 | user request | skills/solai-scaffold/{pick.py (new),serve.py,ui.html}, tests/test_ui.py | **A native folder dialog on the materials step.** A `Choose...` button calls `/api/pick`, which shells out to `pick.py` - a stdlib script that opens the machine's own dialog, prints the chosen path and nothing else, and prints nothing when cancelled. A lock holds while one is open, so a second request is refused rather than queued behind a window nobody can see. The field now takes one path per line, so a multi-file pick becomes several `--materials` flags. 3 assertions (UI-33..35), EXPECTED 35 in that module: 219/219, NOOP unchanged. Version 0.13.0. | The browser's own picker was the obvious answer and is the wrong one: a page is handed bytes and never an absolute path, so using it would have turned copy-from-where-it-is into upload, which is the design the materials step exists to avoid. The server is a process on the same machine and can simply ask. Tk wants the main thread and the server answers on worker threads, so the dialog is a subprocess - the same shape the surface already uses for the engine, and one that cannot take the server down with it. | done |
| 25 | 2026-08-28 | user request | skills/solai-scaffold/{pick.py,serve.py} | **The dialog is now the system's own.** `pick.py` asks Windows for `IFileOpenDialog` with `FOS_PICKFOLDERS` through `ctypes` - the Explorer window, no dependency added - and falls back to Tk only where COM is unavailable. Multi-select is wired for `files` through `GetResults`. `CoTaskMemFree` is called through `WinDLL` rather than `OleDLL`, because a void return read as an HRESULT would raise after the path was already in hand and send a correct answer down the fallback path, raising a second dialog. The server moves to `ThreadingHTTPServer`. Version 0.14.0, 219/219, NOOP unchanged. | Tk's folder chooser works and looks like 2003, and the request was for the built-in one. The COM plumbing is smoke-tested up to the window - create, GetOptions, SetOptions, Release all clean - and `Show` plus `GetResult` can only be proven by a person clicking, which is recorded here rather than claimed. Threading the server follows from the feature: one request is now a window waiting on a person, and on a single thread it would hold the only thread answering the page. | done |
| 26 | 2026-08-28 | operator saw a blurred dialog | skills/solai-scaffold/pick.py | **The dialog is DPI-aware.** `_dpi_aware()` runs before any window is made, trying the per-monitor v2 context, then the older per-monitor awareness, then the system-wide flag. No version bump: nothing an emitter reads changed, and `pick.py` is executed fresh on every click, so the running server picks it up without a restart. | This machine reports 120 DPI - 125% scaling - and a process that has not declared itself aware is drawn at 96 and stretched by the compositor. The blur was not the dialog; it was ours for not saying what we could draw. The call is ignored once a window exists, which is why it is the first thing `main` does rather than part of the COM path. | done |
| 27 | 2026-08-28 | operator saw the wrong label | skills/solai-scaffold/serve.py, archetypes/role/manifest.toml, tests/test_ui.py | **The partition label reads from the declaration again, and the field says it is optional.** Row 19 made the page label the partition input from the archetype's `binds` key, but the payload `serve.archetypes()` builds carried only `prompt` and `example`, so the page defaulted to `Tracks` in an archetype whose codes land in `area`. `binds` is now in the payload and UI-36 holds it there. The role prompt says the field is optional and that the areas usually only become clear once the obligations exist. 220/220. No version bump. | Two defects in one field, and they are the same defect: the surface answering where the declaration had already answered. The label defaulted because a key was dropped in transit, and the prompt read as a required question because nothing in it said otherwise - on a page where every other field must be filled, silence reads as required. | done |
| 28 | 2026-08-28 | user request | skills/solai-scaffold/{serve.py,ui.html}, tests/test_ui.py | **The partition leaves the setup form.** It joins `mode` and `classes` in `INERT`: rendered as a statement with its reason - set later, not at setup - and never sent to the engine, so a new place starts with no areas. The input, and the dead branch that built it, are gone. The command line still takes `partition=` for a place that already knows its divisions. UI-04 now names three inert answers. 220/220, no version bump. | The codes are a description of how the work divides, and nobody has that description on the day the folder is made: it reads out of the obligations once they exist. Left as an input it was a question the page could not help anyone answer, and any answer given would then sit in every card. Shown as a statement rather than deleted, because the field is real and the page saying nothing about it would leave the operator to discover it in the data dictionary. When a second type is offered - `project`, whose streams genuinely are known on day one - this decision has to be revisited per type rather than globally. | done |
| 29 | 2026-08-28 | operator could not tell what to write | skills/solai-scaffold/ui.html | **Two hints that were too short to answer.** `Purpose` now says it is one line in the operator's own words, that it lands in CLAUDE.md, and shows a placeholder. `First deliverable` names the usual answer for a new role - the list of what the role owes, handed to whoever appointed you - and says the date is what everything the place reports is measured against. `area()` takes a placeholder. 220/220, no version bump. | `One line.` describes the shape of the answer and nothing about its content, which is a hint that only helps somebody who already knew. The first deliverable was read as optional ceremony; it is the one field the place cannot compute for itself, because the denominator of the ratio it reports has to be a promise a person made. Saying so on the field is cheaper than defending it later. | done |
| 30 | 2026-08-28 | user request | archetypes/*/manifest.toml, skills/solai-scaffold/{ui.html,scaffold.py}, tests/{test_decl.py,test_ui.py} | **Purpose comes with the type.** Each of the four manifests declares `[interview.defaults] remit`, and both doors take it: the page fills the field when a type is picked and stops the moment anyone edits it, the command line falls back to it instead of `Unknown`. Two assertions - DA-30 that every shipped type declares one, UI-37 that it reaches the page. 222/222, NOOP unchanged. No version bump. | A default belongs in the declaration, not in the surface: written into the page it would be a fifth place the four types are described, and it would drift from them. The offer-until-edited rule is the one the path field already uses, and it is the only version of a default that does not quietly answer for somebody - the sentence is visible, in the field, before create is ever pressed. | done |
| 31 | 2026-08-28 | user request | lib/emit/claudemd.py, archetypes/*/manifest.toml, skills/solai-scaffold/{scaffold.py,ui.html}, common/fragments/claude/80-naming.md (deleted), tests/test_primitives.py | **Language becomes three answers instead of one.** `output_language` still says what the place's own files are written in. `content_languages` is new and multiple - English, Russian, Kazakh - and the CLAUDE.md language region states them, with the rule that a card is written in one of them whole. `filename_language` is new and decides whether a file named for its subject may carry a content language while structure stays English, or whether every name is ASCII kebab. `naming` moves from a seeded fragment to a generated region, because it now depends on an answer; `80-naming.md` is deleted, its text carried into the emitter. Four assertions (LG-01..04). 226/226, NOOP unchanged at 5 / 41 / 29 / 33, stamped counts up one per archetype. Version 0.15.0. | One answer was doing three jobs and could not say the thing the operator actually needed said: that the place is run in English while its content and some of its file names are not. Kazakh is offered as a content language only - there is no `kk` label set, so a place cannot yet be written in Kazakh, and offering it as an output language would assert a capability that does not exist (D1). A convention that depends on an answer cannot stay a seeded fragment: it would be a rule the file states and the answers contradict, which is the drift D10 makes visible everywhere else. | done |
| 32 | 2026-08-28 | user request | skills/solai-scaffold/{serve.py,ui.html}, tests/test_ui.py | **The language section is two questions, and the second is conditional.** `output_language` leaves the form and joins the inert statements: this surface builds places run in English, and the command line still takes `output_language=ru`. `content_languages` stays. `filename_language` renders only when a content language other than English is picked - with English content, a name in the content language IS an English name, and the fork does not exist. Both are chips now, one helper for the single-choice and the multiple-choice shape, writing the value in declared order rather than click order. 226/226, no version bump. | Row 31 shipped three answers where the operator could see two: what a place is written in and what its content is written in are the same question for anyone who is not deliberately splitting them, and the split belongs on the command line where it is rare and deliberate. A control that only sometimes asks something is better than one that always asks and is sometimes meaningless: the second is how a form teaches people to answer without reading. | done |
| 33 | 2026-08-28 | user request | skills/solai-scaffold/ui.html | **The language options are bigger, and they stop fighting the plan pills.** Padding 6/14 to 11/22, 13px to 14px, gap 8 to 10 - the same height as the other buttons on the page. The class is renamed `chip` to `opt`: the type cards already owned `.chip` for the plan pills, and the two rules were overlapping, with the later one winning some properties. 226/226. No version bump. | The collision is why they looked wrong rather than merely small: a monospace 11px pill style was landing on half of a control meant to be pressed. Two things with one class name is the same defect as two things with one ID, and it was introduced two rows ago by reaching for the obvious word. | done |
| 34 | 2026-08-28 | user request | skills/solai-scaffold/{ui.html,scaffold.py}, lib/emit/bond.py, archetypes/*/manifest.toml | **The first-artefact control and the inert statements leave the page.** Each type now declares `[interview.defaults] first_artefact`, the page takes it the way it takes the purpose, and the block of `mode` / `classes` / `partition` / `output_language` statements is no longer printed - its reasons live in `serve.INERT` and here. The `artefact()` and `compose()` helpers and the `.inert` styles are deleted with it. New: the bond records `first-artefact-by: default | operator`, and prints `(the type's default. Nobody has promised this yet.)` next to an artefact no person named. 226/226, NOOP unchanged. Version 0.16.0. | The field could not simply vanish. `check_caps` refuses an empty folder more than four system files and one skill unless an artefact is named, so a role place - five and nine - is refused outright without one; the promise is what buys the structure, at every tier, not only at standard. Moving it to a default keeps the page short at a real cost, and the cost is now written where it will be read rather than argued about: a place built this way says on its own front page that nobody has promised anything yet. | done |
| 35 | 2026-08-28 | defects found in the first real place | lib/engine.py, skills/solai-scaffold/scaffold.py, skills/solai/tiers.md, tests/test_primitives.py | **Two fixes, both found by building something.** `engine.promise_provenance(given, default)` compares the first artefact against the type's own sentence, so a page that posts the default no longer looks like a person who typed it; row 34 shipped the flag and got it wrong on the first place built. `tiers.md` said the tier decides which files exist - it never has: the archetype's manifest decides artefact by artefact, and a `role` place at `light` carries its data dictionary and validators because `role` declares them. The table now says what the tier actually governs, and names the empty-folder cap as the one thing it cannot negotiate. Two assertions (LG-05, LG-06). 228/228, NOOP unchanged. Version 0.16.1. The first place, `SMP`, was re-planned and re-applied: one WRITE, the bond, now reading `first-artefact-by: default`. | The honesty flag was itself dishonest for exactly one version, which is the argument for building something with the tool rather than reading it: nothing in 226 assertions could catch a label that was right in every case except the one the surface actually produces. The tiers text is the older defect - documentation describing a filter that was never written, believed for eight versions because nobody had asked a light place what it contained. | done |
| 36 | 2026-08-28 | user request | lib/emit/starthere.py (new), lib/engine.py, archetypes/*/manifest.toml, skills/solai-scaffold/ui.html, tests/{test_primitives.py,verify_engine.py,README.md} | **Every place now ships START-HERE.md.** A ninth emitter, wholly derived: the remit, how to open a session in that folder, the inbox instruction where an inbox is declared, the first three steps of the manifest's own loop as a table, what finishing session one looks like (one card of the first class naming its source, an empty inbox, a clean check), the standing line about a promise nobody made, and where the rules, the schema, the state file and the dashboard live. The completion panel now points at it rather than trying to be it. 4 assertions (SH-01..04), NOOP 6 / 42 / 30 / 34, 232/232. Version 0.17.0. The first real place, `SMP`, was re-applied and has it. | The page is a tab that gets closed; the place is not. A person who pressed create was left with a folder, a dashboard of zeroes and no next move, which is the failure mode the whole package is aimed at - structure handed over with nothing telling anyone what to do with it. Generated rather than seeded, because a paragraph written once per archetype drifts from the archetype: the loop in this file IS the loop in the manifest, and it cannot say otherwise. Separate from `_system/state.md` on purpose: that one is seeded once and owned by the person, and mixing generated content into a file somebody edits is the ownership defect D10 exists to make visible. | done |
| 37 | 2026-08-28 | operator clicked a button that did nothing | skills/solai-scaffold/{serve.py,ui.html}, tests/test_ui.py | **The dashboard opens, and Copy path becomes Start a session here.** `Open dashboard` linked `file:///...`, which a browser silently refuses to follow from an `http://` page - the click did nothing and said nothing. There is now a `/dashboard` route: the server reads the file and serves it, since it is a process on the same machine as the file. `_authorised` matches the key as a parameter rather than as the tail of the URL, which is what a second parameter broke. `Copy path` is replaced by `Start a session here`: `/api/start` opens PowerShell in a new console, `Set-Location -LiteralPath` into the place, then `claude`. Quoting doubles a single quote, so a folder called `Bob's place` opens rather than ending the argument. 3 assertions (UI-38..40). 235/235, NOOP unchanged. Version 0.18.0. | A path on the clipboard is a step the person still has to perform, and the step is the one where the session gets opened in the wrong folder - where the place's skills do not exist. The dashboard defect is the more interesting one: it had been shipped and described as working, and no assertion could have caught it because the failure lives in the browser's security model rather than in this code. It took a click. | done |
| 38 | 2026-08-28 | user request | skills/solai-scaffold/ui.html, tests/test_ui.py | **The tagline is deleted and the rail has three states.** `Built exactly as declared.` is gone with its style; the wordmark keeps the spacing. The rail distinguished satisfied from unsatisfied, and half the steps satisfy themselves - the name is prefilled from the type, the purpose from the type, the languages and the tier from defaults - so it filled with ticks nobody had earned. It now shows a tick for a step a person answered, a dashed dot for a step that needed nothing, the number for a step still waiting, and the current step raised. A step is also clickable and scrolls to its own section when that section is open. 1 assertion (UI-41). 236/236. Version 0.18.1. | The rail was reading state correctly and reporting it uselessly: `answered` and `nothing needed here` are different facts, and collapsing them into one tick is the same defect as a status field that cannot tell `open` from `not applicable`. The click-to-scroll is deliberately weak - it navigates to an open step and never opens one, because opening a step is what answering the one before it does. | done |
| 39 | 2026-08-28 | user request | skills/solai-scaffold/ui.html | **The completion panel is the heading and two buttons.** The path-and-counts line (`...\SMP - NOOP 20 WRITE 14`) and the sentence pointing at START-HERE.md are deleted, with the `.path` style and the `counted` parse behind them. 236/236. No version bump. | The counts had already been read in the plan table directly above, the path is on screen at step 3, and the pointer to START-HERE.md was telling somebody to open a file by hand next to a button that opens the folder for them with a session running in it. Each line was true and none of them was needed. | done |
| 40 | 2026-08-28 | user request | skills/solai-scaffold/serve.py, tests/test_ui.py | **The session opens with something on the screen.** `start_argv` passes `FIRST_PROMPT` to `claude` as its opening message: read START-HERE.md and CLAUDE.md, list what is in `_inbox/`, then say in five lines what this place is for, what is waiting to be read and what the first move is - and write nothing yet. 1 assertion (UI-42). 237/237. Version 0.19.0. | The button solved the wrong half. It put a terminal in the right folder and then asked the person to know what to type, a minute after pressing create, which is precisely when they do not. The prompt ends in `Write nothing yet` on purpose: a session that starts by making cards has skipped the reading, and the reading is the step that decides what the cards should say. | done |
| 41 | 2026-08-28 | user request | skills/solai-scaffold/ui.html, tests/test_ui.py | **A craft pass over the setup surface, and the first one made with the page in view.** The page had never been seen rendered: it binds to loopback, which the browser tooling cannot reach. Rendering the initial state to a static file and shooting it with headless Chrome ended that, and the first frame showed the defect. The four types shared one grid, so the three this surface does not build took the whole top row and the only one it does was orphaned underneath with half a row of dead space beside it. They are now two groups: what is offered leads and widens to 560px when it is the only one, and the rest sit below a rule as reference, with one shared reason instead of the same amber sentence printed three times. Also: a step you have not reached keeps a readable heading rather than being dimmed whole; the rail no longer reports a step ahead of you as needing nothing; the rail and the tier rows answer a keyboard and show a focus ring; the tier rows are buttons; a reduced-motion block; seven count tiles on one row; and the stale badge no longer sits on top of the first line of the plan it describes. Two assertions (UI-43, UI-44), both shown to fail before they were kept. 239/239, NOOP unchanged at 6 / 42 / 30 / 34. Version 0.19.1. | Every visual judgement about this page had come from the operator looking at a screen and saying so, which is a slow loop and an unrecorded one. Chrome is on this machine and the page paints its first state without an API call, so there was no reason the surface could not be looked at directly. One real bug came out of it that no amount of reading would have caught: `font: 14px/1.45 inherit` is not a valid shorthand, `inherit` cannot stand in for the family, so the whole declaration was dropped and the browser default of monospace for a textarea came through underneath. Purpose is a sentence about the work and had been rendering as though it were a path. The type grid is the same class of error as the rail ticking itself in row 38: the page was reporting something true, in a shape that told the reader the wrong thing. | done |
| 42 | 2026-08-28 | user request | skills/solai-scaffold/{serve.py,ui.html} | **`place` retires as the noun for the thing being built, across the whole surface.** Fourteen occurrences in `serve.py` and six in `ui.html` become `vault`, including the three a person actually reads: the materials step now says documents prepared for this vault, the Purpose hint asks what this vault is for, and the completion panel says your role vault is ready. The opening message handed to a new session says vault too. Two are deliberately kept: the comment about a surface saying one thing in one place and doing another elsewhere, and the role type declared opening line, which ends in one place, because both are the English idiom rather than the noun. 239/239, NOOP unchanged. Version 0.19.2. | The package called the thing it builds a vault 275 times and a place 204 times, and the split ran along a seam: the persona skill said vault almost exclusively, this surface said place almost exclusively, so which word a person met depended only on which door they came through. By D1 that is a defect and not a synonym. Vault wins because it is what the persona, the bond filename and the operator already say, and because place was only ever the old package name leaking into the noun. The rest of the package still carries the word: `starthere.py` writes it into every generated START-HERE.md, and the change records keep it because a record is immutable once written and says what was true when it was written. | done |
| 43 | 2026-08-29 | user request | repository root, .claude-plugin/marketplace.json, doctrine.md, lib/emit/classes.py, README files, skills/solai-adopt/EXAMPLE-*.md | **The package gets a repository of its own, and an installer.** `git init` at `~/.claude/solai`, MIT licence, a root README that opens with the install line, and `install.ps1`: eight idempotent stages taking a bare Windows machine to a working vault (winget for Git, Python, Obsidian and the GitHub CLI; the native Claude Code installer; `plugin marketplace add` then `plugin install`; an optional private-config restore behind `gh auth login`; a wait on OneDrive; `claude doctor` plus the test runner; then the guided page). Prepared for publication: the two conformance reports over real vaults leave the tree, three prose references to a client project are neutralised, and the marketplace declaration is repaired - its plugin `source` still pointed at `./plugins/place`, a folder that has not existed since v0.9.0, so the install line printed in the package's own README could not have worked. 239/239. | The package existed on one disk. The allow rule in the config repository still named `place/`, so nothing since v0.8.0 had ever left this machine, and the tracked `place/` tree read as 97 pending deletions that the next `git add -A` would have pushed - emptying the only remote copy. A public repository makes the package distributable and reviewable; the installer makes a second machine a paste rather than a project. | done |
| 44 | 2026-08-29 | bare-machine test | install.ps1 | **The installer survives a machine that has nothing on it.** A run against a simulated bare profile (empty home, stripped PATH) died in stage 0: on a fresh Windows the Python launcher writes `Python install manager was successfully updated` to stderr, and under `$ErrorActionPreference = 'Stop'` a native command's stderr is a terminating error. Every native call now goes through `Invoke-Native`, which lowers the preference for the duration and judges by exit code, and PowerShell 7 gets `PSNativeCommandUseErrorActionPreference = $false` as well. Three further findings from the same run: `python` on a bare machine is usually the Store app-execution alias, on PATH and not an interpreter, so the probe now accepts a version only when it parses and falls back to `py -3`; winget exit 1602 is reported as *the elevation prompt was dismissed* rather than a package failure, because that is what it means and it happened; and the private-config restore no longer requires the GitHub CLI, whose installer is machine-wide and cannot be installed without admin - it uses Git Credential Manager, which ships with Git for Windows and signs in through the browser, and reaches for `gh` only when it is present and already logged in. | The whole design rests on one line working on a machine that has nothing. Every branch verified until now was an *already installed* branch, and the first test of the other side found a stage-0 death, a false positive on Python, a misreported cancellation and a dependency on a tool this very machine could not install. | done |
| 45 | 2026-08-29 | user request | skills/solai-scaffold/ui.html, tests/test_ui.py | **The folder a vault will live in can be chosen from the system dialog, like the materials folder already could.** Step three had a text field and a Check button; step four, one step below it, had a `Choose...` button onto the same `/api/pick` endpoint and the same native Explorer dialog. The button is now on both. What the dialog returns is treated as the folder the vault goes *in*, because the vault does not exist yet and a folder chooser can only name a folder that does: the name from step two is appended, unless the chosen folder is already called that, which is what picking an existing vault looks like. New assertion UI-45 holds both pickers to the page, 240/240. | A person setting up a machine had to type an absolute Windows path by hand into the one field that decides where everything lands, while an identical field one step below offered a dialog. That is the kind of asymmetry a surface should not have, and typing a path with two kinds of slash and a space in `Obsidian Vaults` is where a bring-up stalls. | done |
| 46 | 2026-08-30 | setup run, blocked at step four | lib/engine.py, skills/solai-scaffold/{serve.py,SKILL.md}, tests/test_primitives.py | **A folder of materials is mirrored into the inbox instead of being refused for holding one name twice.** A real setup stopped dead on `two files are called Схема рассадки Қазақстан Тарихы (1).docx`, one in a folder and one in its own subfolder - ordinary in any tree a person has kept for years, and the message named two absolute paths and no move. `collect_materials` now returns pairs of (source, the name the file takes inside the vault) and mirrors a walked folder's tree, so both documents arrive under the subfolder each came from and neither overwrites the other. A name is refused only when two of the given paths claim it at the same relative place and their own folder names cannot tell them apart either, and that message now says what to do: hand in the one folder that holds both, or rename one. `place` becomes `vault` in the remaining engine refusal. The page lists the inbox name rather than the basename, because a mirrored subfolder is the reason two files of one name can both be there. 2 assertions added or rewritten (MAT-01, MAT-03 inverted, MAT-06 new), EXPECTED 61 → 62: 241/241, NOOP unchanged at 6 / 42 / 30 / 34. The reported folder, 2 092 files with 51 repeated basenames, now collects clean. Version 0.19.3. | The refusal was written to stop a silent overwrite, and that instinct was right - but it defended the file at the cost of the person, who was handed a blocked step, two paths and no verb. Flattening was the actual defect: the inbox took a basename from a tree that had already answered the question of what distinguishes two documents of one name, which is the folder they sit in. Mirroring keeps that answer and makes the refusal rare enough to be worth its words. It also hands the first session more than bytes: the shape the person put their documents in is evidence about them, and flattening threw it away. | done |
| 47 | 2026-09-03 | counselor pilot, CHG-026/027/072 | archetypes/role, lib/decl.py, lib/emit/cardskill.py | **The class set the first real `role` vault arrived at.** `obligation` -> `duty` (`DTY`, `duties/`); `decision` still declared and still installable but no longer scaffolded; `deliverables/` stops calling itself "the only evidence the role produced anything". `filename = "bare-id" \| "slug"` becomes a declared per-class field, and `CLASS_KEYS` with an unknown-key check means a class declaration can no longer carry a key nothing reads. Minting scans `id:` instead of globbing filenames. | Five days of the only real run of this archetype. All 16 obligation cards were `kind: standing` with no charter naming them, and 15 of 16 already used the word duty. `decision` held zero cards after the whole archive had been read. A graph view of sixteen nodes reading DTY-001 to DTY-016 is useless for the one thing that view is for - and the vault's own `filename = "slug"`, written to fix it, was accepted and read by nothing. That last point is why the unknown-key check is in this row and not a later one. | done |
| 48 | 2026-09-03 | counselor pilot, CHG-064/067/068/071/072 | runtime/ | **The checks the pilot had to fork or invent, brought upstream.** `validate_cards.py` reads identity from `id` and resolves links through `aliases`, and gains an alias index the fork lacked; AV-3 reports `not applicable` where no class carries what leaves, instead of a zero. `check_links.py` retires the word "orphan" for four named counts and reports attachments nothing points at. New `check_binaries.py` asks whether a binary is described, and new `measure_cards.py` makes `card-concision.md`'s instruction executable. | The package validator reported 46 BLOCKERs against the pilot and its own shared fragment named a script the package did not ship. The fork was cherry-picked, not copied: it had silently reverted the CL-5 optional-field hint. AV-3 had reported "0 promised, 0 sent" on every run since the vault was built, including four days when the class was retired and the folder deleted. | done |
| 49 | 2026-09-03 | running the suite | tests/ | **`verify_engine.py` was red on the committed repo and is green.** Two causes, both bookkeeping: the `reader` agent shipped in the role manifest without `CASES` following it, and the gate assertion failed on any GATE at all. It now fails on a BLOCKER, or on a gate outside the set a freshly built place is supposed to raise. `tests/README.md` had drifted to 178 against a suite of 241 and omitted a whole module. | A verification that cannot be green is not a gate. `validate_cards.py` exits non-zero on BLOCKER alone, and AV-2 says of itself that it is reported every run until it is not true, so demanding zero gates from a place that has by construction shipped nothing was a category error. | done |
| 50 | 2026-09-03 | counselor pilot, CHG-067 | lib/decl.py, lib/engine.py, skills/solai-scaffold/scaffold.py, runtime/stamp_check.py, tests/test_compiled.py | **A place regenerates from its OWN declarations, not from the package it was built from.** `_system/os/manifest.toml` is compiled in beside the classes, agents and workflows that already were, and `decl.load_compiled` reads the lot - discovering classes by listing the directory, so deleting a declaration IS the retirement and adding one IS the addition. A place that has been built before is resolved vault-local; one built before this release has its manifest synthesised on the next apply. `--from-package` re-imports deliberately. A hand-edited region gains three fates instead of one: RECONCILED where its content is what the declaration now produces, ADOPTED where `--adopt <region> --because CHG-NNN` has signed for it, and HAND-EDITED otherwise; `--diff <region>` shows current against would-be-generated. A skill projecting an undeclared class is reported as a deferral naming the D7 fates, never deleted. | The vault's own words: "CLAUDE.md says its generated blocks must not be hand-edited: edit the declaration instead. The declaration is not in this vault." Four blocks went wrong and none could be corrected, so it wrote a hand-maintained section listing them - and two were silently re-stamped over hand edits, which turns a detected defect into an undetectable one. Nobody does that with a way out available. | done |
| 51 | 2026-09-03 | counselor pilot, CHG-002/003/023/040/064/065/067/068/074 | doctrine.md, lib/emit/claudemd.py, lib/emit/vocabulary.py, lib/emit/bond.py, common/fragments/system/scripts-readme.md, three manifests, skills/solai-scaffold/scaffold.py | **The rules a place on a real archive has to invent, and one it should never have had to.** Doctrine gains **D19**, CORE: nothing is deleted by a script, release is a person's own act, test the copy rather than the name, and selection for deletion runs on per-file evidence and never on a field describing what a file is about. The `naming` region gains the binary rule - ASCII kebab whatever the content language, the document's own name in `title:`, one document one name, 60 characters, and a rename is not finished until every reference is rewritten. `file` and `folder` are declared in the vocabulary, because `check_binaries.py` reads both by default. `_system/scripts/readme.md` is seeded with the run-order contract. The `corpus` answer is counted and recorded in the bond instead of being asked and discarded. | The `naming` region told every archive-bearing place that a subject-named file may carry spaces and a second alphabet; one place answered that question, renamed all 308 of its binaries to ASCII kebab anyway, and could not correct the block. `CLAUDE.md` had promised a run-order contract in that readme since the first release and the package shipped no readme. D19 is the only principle here added from a place that was running rather than one being read, and it is the only one whose failure cannot be answered by the next change record: the rest protect a claim, D19 protects the evidence. | done |
| 52 | 2026-09-12 | DLV-001, closing GAP-002 | skills/solai/author.py, lib/fsplan.py, skills/solai/{SKILL.md,authoring.md}, tests/test_authoring.py | **`class add|rename|retire` ships, and `fsplan` learns to delete.** Adding a card class was the last operation still done by hand-writing TOML, and the counselor pilot changed its class set four times in twelve days that way. `add` renders the declaration from arguments and validates it through `decl.load_class` in a temp file before anything is planned; it builds the view columns and the enum rubrics rather than asking, because the validator rejects a column that is not a declared name. `retire` refuses while anything still names the class and prints each hit across four surfaces. `rename` rewrites only the six keys that hold a name. `fsplan.Plan.delete` is new: before it, the only honest retirement was a hand `rm`, which leaves no row in the table and nothing to roll back to. 19 new assertions, 285/285. Version 0.23.0. | The retirement check is the whole point, and it is not a hypothetical: run against `role`, it reports that `runtime/validate_cards.py` hardcodes `DLV`, which is exactly how `CHG-008` of the counselor vault left `AV-3` counting a class that had been deleted. The gate read zero whatever that role shipped, for sixty-four records, and ten commits filed it as a standing finding before anyone read it as a broken circuit. | done |
| 53 | 2026-09-12 | PAN-002 | runtime/check_sensitive.py, archetypes/*/manifest.toml, common/fragments/system/scripts-readme.md, tests/{test_sensitive.py,run_tests.py,verify_engine.py} | **A personal-identifier guard ships with every vault that carries scripts.** `check_sensitive.py` walks the whole tree rather than a declared list, and sorts what it finds by what git would do with it: EXPOSED (untracked and unignored, one `git add` from permanent) is the only state that turns the exit code red; TRACKED is a decision someone made; ignored has no path to a commit. It never prints an identifier it found, and it counts binaries as unscanned rather than passing over them in silence. Nine assertions, 304/304. The three card archetypes gain one plan row each. Version 0.24.0. | The package had seven runtime checkers and none of them looked for personal data, while the one vault holding it met `CHG-134`: a sync client conflict copy carrying 728 children's identifiers, untracked, unclaimed by `git check-ignore` because the ignore rule named the page it copied by its exact filename. The guard that vault then wrote is named in three of its change records and is not on disk anywhere today. It ships to every archetype rather than on an interview answer, because the failure is a file nobody declared and no declaration would have mentioned it. | done |
| 54 | 2026-09-12 | GAP-004 | common/fragments/system/gitignore.txt, archetypes/*/manifest.toml, tests/{test_sensitive.py,verify_engine.py} | **Every vault is born with a `.gitignore`, and the rule in it is a glob over a name.** Seeded, so it is the vault's own from the first write. It carries the machine-local entries, Obsidian's per-device editor state, and one convention: a file holding personal data carries `sensitive` in its NAME, and `*sensitive*` covers it at any depth before it exists. All four archetypes take it, `minimal` included. Two assertions, 306/306. | `CHG-134` had two halves and row 53 shipped only the first. The rule that failed there named a page by its exact filename, so it was correct about the file it named and useless about the copy a sync client wrote beside it. A list of filenames can only ever be right about files that already exist. A marker in the name is right about the ones that do not. What still cannot be matched safely is OneDrive's own `name-MACHINENAME.ext` shape, which would swallow legitimate files, and the fragment says so rather than implying coverage it lacks. | done |
| 55 | 2026-09-12 | PAN-003, fourth sighting | runtime/mint_change.py, archetypes/*/manifest.toml, skills/solai/SKILL.md, common/fragments/system/scripts-readme.md, tests/{test_change.py,run_tests.py,verify_engine.py} | **A change number is claimed by creating the file, not by reading the folder.** `mint_change.py` opens the next free `CHG-NNN` with `O_CREAT | O_EXCL` and writes a stub into the handle, so the check and the claim are one syscall and a loser simply takes the next number. Holes are never filled. The change folder is read from the vault's own `answers.toml`. `STEP 7` now forbids the model minting a number itself. Seven assertions, one of which races sixteen threads against a real filesystem; 313/313. Version 0.25.0. | The counselor vault lost two records this way, recovered them from a session transcript that happened to still be open, and `CHG-118` there called that luck rather than a property of the system. It prescribed "read the folder immediately before the write", which is prose: it shortens the window between the read and the claim and cannot close it. `CHG-148` reports the same collision thirty records later, and a fourth was found on 2026-09-12 sitting on a sync-conflicted branch. Four sightings in ten days is not a discipline problem. | done |
| 56 | 2026-09-12 | a release that shipped a package which could not import | skills/solai-release/{release.py,SKILL.md}, skills/solai/SKILL.md, tests/{test_release.py,run_tests.py} | **`/solai release`, whose feature is the ORDER.** The three gates run last, immediately before the push, and a dirty working tree is refused outright. It also refuses a tag that exists, a version no CHANGELOG row claims, a branch behind its target, and a branch that is not ahead - each naming itself rather than exiting on a number. It does not merge, because a conflict is a decision. After a successful push it prints the obligation it cannot discharge: a deliverable card owed `sent-on`. Six assertions, 319/319. Version 0.26.0. | Earlier the same day a release ran all three gates, then bumped a version, then committed. The bump truncated `lib/__init__.py`, the package could not import, and the commit went in with three green gate results above it in the transcript. Nothing lied: the gates were green when they ran and then the tree changed. That is not a discipline failure to be solved by remembering harder, it is an ordering requirement, and an ordering requirement belongs in a script. | done |
| 57 | 2026-09-12 | GAP-005, the first real upgrade | lib/decl.py, lib/engine.py, skills/solai-scaffold/scaffold.py, tests/{test_compiled.py,verify_engine.py,README.md} | **An upgrade MERGES.** `--from-package` re-imported the archetype whole, so every class the vault declared and the package does not carry was absent from the load and the upgrade regenerated the card index, the dictionary, the vocabulary and the base without it. `decl.load_merged` is the third loader beside `load_archetype` and `load_compiled`: package-only is taken, vault-only is kept with its folder and its skill, and where both declare a class the PACKAGE wins and the note names it - reported only where the two files differ, because a note against every unchanged class buries the one row that matters. The merged manifest is rendered rather than copied, since the package file lists five classes with six sitting beside it. `_compiled` splits the reading from the set check, so an upgrade is not refused on account of a state it resolves. Seven assertions and a sixth engine scenario, `upgraded`, the mirror of `evolved`: 326/326, plan-row counts unmoved. Version 0.27.0. | The flag exists because an emitter fix travels on a plain `--apply` and a declaration fix does not, so `--from-package` is the only route a package declaration change has into a vault already built - and it was the one command that deleted a class. This vault declares `deliverable` by the decision in its own `CHG-004`; the counselor declares `platform` and `subject`. Upgrading either deleted the class and left its cards on disk with nothing validating them, which is the worse half. D19 says nothing is deleted by a script. The interesting defect was found by the end-to-end scenario against code the unit suite had just passed: the source hash named the manifest at `arch.root`, the package copy, while the vault held the widened one, so every generated region read STALE on the next plain run. That failure lives in the relationship between two runs and no assertion about one run could see it. | done |
| 58 | 2026-09-12 | the residue named in row 57, closed rather than carried | lib/decl.py, skills/solai-scaffold/scaffold.py, tests/{test_compiled.py,verify_engine.py,README.md} | **The merge covers all four kinds, not just classes.** `load_merged` applies the same three rules to lookups, agents and workflows: package-only taken, vault-only kept, both to the package. `consider` is one function over the four, and only the manifest lists that actually gained a name are rewritten, so a key this merge never touches cannot be refused for a shape it never had to parse. A lookup on the vault's side stays a lookup, because which list a declaration belongs to is the vault's statement about it and the package has no opinion about a name it does not carry. The `upgraded` scenario now declares an agent and a workflow of its own alongside the class, and demands the compiled declaration, the projection under `.claude/`, the manifest row and idempotence for each. Four assertions, 330/330; both gates shown red with the merge removed before they were kept. `tests/README.md` was stale in six more places and is read off the harness again. Version 0.28.0. | Row 57 guarded classes and wrote the rest down as uncovered: "no vault does, so this is named here rather than guarded." That is an argument about today's vaults and not about the rule. An agent is compiled into `_system/os/agents/` exactly as a class is, `load_compiled` discovers it by listing the directory exactly as it discovers a class, and a vault that writes one has done nothing the engine tells it not to. The failure would have been identical and the excuse would have been worse for having been written down and shipped. A named residue is a decision to fail later on purpose. | done |
| 59 | 2026-09-12 | a reference architecture handed in for comparison | ARCHITECTURE.md, ROADMAP.md, README.md | **The package describes its own stack, and the roadmap `kb_build.py` has cited since it was written now exists.** `ARCHITECTURE.md` sets out nine layers, what each one is here rather than in the abstract, and the one boundary the rest follows from: solai is not a runtime, Claude Code is, so an agent is a declaration the harness executes and there is no orchestrator to build. Each layer names what holds it honest, and the two thin ones say so on their face - the knowledge layer ships three scripts with no skill surface, and observability has a ledger and no traces. `ROADMAP.md` carries fifteen rows across five statuses, and every `declined` row names what would reopen it. `README.md` said doctrine holds 18 principles against 19, and its `Layout` omitted `solai-release/` and `solai-kb/`. No engine change, 330/330, no version bump. | The comparison was useful in one direction only: four of the six layers a generic multi-agent architecture proposes already exist here in another form, and the two it would have added - an asyncio router and a vector store - are both things this package has reason not to build. That is worth writing down once, because the argument will arrive again. The finding that came out of the exercise is not in the new files' favour: `solai-kb` has three substantial scripts, a SQLite index and four build refusals, and nothing in the package routes to it. A handoff note recorded that on 2026-09-03 and it has been shipping unreachable since. A package that enforces D1 and D18 on every vault it builds was failing both in its own tree. | done |
| 60 | 2026-09-12 | ROADMAP R-1 | skills/solai-kb/{SKILL.md,kb.example.toml}, tests/{test_kb.py,run_tests.py,README.md}, ARCHITECTURE.md, ROADMAP.md, skills/solai/SKILL.md, README.md | **The knowledge layer is reachable.** `skills/solai-kb/` gets the surface it has been shipping without: a `SKILL.md` with three verbs, the three homes the build keeps apart, the seven refusals each naming itself, and `--dry-run` before a real build by the same rule that puts `--plan` before `--apply`. `kb.example.toml` is the exemplar the rules file is copied from, since no `kb.toml` existed anywhere in the tree to read the schema off. `tests/test_kb.py` builds a vault in a temporary folder and asserts the selection, the refusal list in the manifest, the four artefacts on disk, the anchor that makes a citation exact, and that the same corpus and the same builder rebuild to the same bytes: 25 assertions, 355/355. Two are structural and say so on their face - the cloud placeholder gate, because no test can make a file Windows reports as living elsewhere, and nothing at all about whether the selection rules select the right documents. `/solai` names `kb` in its verb list and points at the skill, within the 199 lines it already had. No engine change, no version bump. | A package that enforces D1 and D18 on every vault it builds was failing both in its own tree: three substantial scripts, a SQLite index and four build refusals, and nothing routing to any of it since 2026-09-03. The test module is the part that was owed rather than the skill file. The dangling-anchor gate looked decorative until the suite found the case that reaches it - two documents declaring one `id`, where the last one read owns the text and the records of the first still claim an anchor it does not carry. That is a real corpus fault, and it is now the assertion. | done |
| 61 | 2026-09-12 | R-1 follow-through | tests/{verify_engine.py,README.md}, ARCHITECTURE.md | **The knowledge layer is exercised end to end.** `answerable` is the third scenario after the four archetypes, beside `evolved` and `upgraded`: two cards and a two-level document are written into a `project` vault the engine built, and a knowledge base is built out of it into a folder outside the vault. It demands 4 records of the three expected kinds, the block anchor surviving into an anchored citation, no record whose path starts `.claude/` or `_system/`, an identical `records_hash` on a rebuild, retrieval returning the card that carries the question's words, a refusal by name when the output would land inside the vault, and every file in the vault hashed before and after. Each assertion was shown red before it was kept: widening the selection to `_system/**` raised both the count and the leak; removing the anchor from the fixture raised the citation check; a line writing one byte into the vault raised the boundary check by name. `tests/README.md` also said 0 GATE where a fresh place is supposed to raise `AV-3`. No engine change, 355/355, plan-row counts unmoved, no version bump. | Row 60 gave the layer a surface and 25 unit assertions over a corpus the test invents. What no unit assertion could reach is the pairing: whether a vault this engine produces is a corpus this build can read, and whether the build leaves it alone. The second is the layer's one real boundary - three things, three homes - and it was carried by a docstring and a single `if` in `main`. A boundary asserted by hashing every file before and after is the only form of that claim that survives a refactor. | done |
| 62 | 2026-09-12 | RES-007, from the R-2 migration plan | lib/decl.py, tests/{test_compiled.py,verify_engine.py,README.md}, ARCHITECTURE.md | **An upgrade discovers what a vault declares by listing it, never by trusting a manifest the vault may predate.** `load_merged` read a missing `_system/os/manifest.toml` as "nothing to merge with" and returned the package archetype whole. That is right for a first build and is a silent deletion for every vault built before the manifest existed, which is the set of vaults an upgrade is for. `_discover` is lifted out of `_compiled` and takes `listed=None` where there is no manifest to compare against; `_declared` uses it to read the same directories and returns `None` only when nothing at all is declared, which is what tells a first build apart. Four assertions, `CP-26` to `CP-29`, and a fifth scenario, `predates`, which removes the manifest and re-runs the upgrade. Each was shown red first: reverting the loader turned CP-26, CP-27, CP-29 and three `predates` checks red, and making the empty case speak turned CP-28 red. `CP-28` also passed before the fix and is named as the case the fix must not change. 359/359. Version 0.29.0. | Found by running R-2's migration plan for the Counselor vault and reading the line it printed: `0 declared only by this vault, kept: none`, against a vault that declares `platform` and `subject` by its own `CHG-108`. `GAP-005` was closed for vaults built at or after the version that compiles a manifest, and the one vault it was written for is older than that. The guard switched itself off in the only case that has ever needed it, and no scenario could see it because every scenario builds its vault at the current version. The fix also makes the Counselor's upgrade refuse rather than proceed: both its declarations carry `kind = "reference"`, which has never been one of the four link kinds, so they have been invalid since they were written and nothing loaded them to find out. Refusing is the correct behaviour and is not the same as being migratable; `R-2` stays open. | done |
| 63 | 2026-09-12 | RES-008, from the R-2 migration attempt | lib/decl.py, tests/{test_compiled.py,verify_engine.py,README.md}, ARCHITECTURE.md | **A kept class carries the folder it lives in.** `load_merged` widened four name lists and took `[[folders]]` from the package untouched, so a class the package never heard of, living where the package never heard of, was kept by the merge and then rejected by `_validate_set` for a folder nothing declared. The kept class's folder is now appended to `arch.folders` and written into the merged manifest as a `[[folders]]` entry carrying that class's own `purpose`, inserted after the last folder block rather than at the end of the file. `_covered` is the one spelling of the containment test, shared with the validator, so the merge cannot add a folder the validator rejects or skip one it demands. Three assertions, `CP-30` to `CP-32`, and the `upgraded` scenario's kept class moves from `registry/zeta` to `zetas`. 362/362. Version 0.30.0. | Found by attempting the migration that `RES-007` was written to unblock: the counselor's `platform` and `subject` live at `platforms/` and `subjects/`, and the upgrade stopped there. This is the third thing in the category of what a kept declaration NEEDS rather than what it IS, after lookups-agents-workflows and after the manifest itself. It survived two releases about the merge because the fixture's class sat at `registry/zeta`, under a folder `project` declares for its own reasons, and the one live vault's own class sits in `deliverables/`, which `project` also declares. A fixture that shares an accidental property with the only real example will confirm whatever that property permits, which is why the fixture moved rather than a new one being added beside it. | done |
| 64 | 2026-09-12 | RES-009, from the Counselor apply and its rollback | lib/decl.py, tests/{test_compiled.py,verify_engine.py,README.md}, ARCHITECTURE.md | **Where both declare a class, the package wins on the engine's shape and the vault keeps what its cards are called and shown by.** `VAULT_OWNED` is the closed list - `filename`, `view`, `purpose`, `title` - and `_differing` compares the two declarations by PARSED value rather than by text, which is what the caller had already done and is too coarse to act on. A class differing only in vault-owned keys resolves to the vault's file, whole, comments and all; one differing anywhere else resolves to the package's, and the note names both sets so the reader sees why the package won and what of theirs is replaced. Whole files rather than a synthesised merge, because these declarations carry their reasons in comments above the keys and no writer in the standard library preserves a comment. Agents and workflows have no owned list and keep the older rule, stated in `CP-36` so its absence is a decision. Four assertions, and the `upgraded` scenario's vault now edits a class the package also declares. 366/366. Version 0.31.0. | Found by applying the Counselor migration that 0.29.0 and 0.30.0 had unblocked, and rolling it back twenty minutes later. That vault's `duty.toml` declared `filename = "slug"` with the reason written above the key: the graph view shows filenames, and sixteen nodes reading `DTY-001` to `DTY-016` make the one view built for seeing the shape of the work useless for it. Its own `CHG-027` records the decision. The upgrade replaced the declaration and `validate_cards.py` went from `BLOCKER 0` to nineteen, one per duty card. The escape offered until now was `class rename`, which is no escape for a class the content is built on. The shape was chosen after measuring both vaults' classes key by key rather than before: `duty` differs in exactly `filename` and `view`, and `deliverable` differs in `links` and `status` as well, in the wording of a `meaning` and some `notes` and in no structure, which is the package's to update. A finer prose-level rule would have kept that wording too, and nobody has asked for it. | done |
| 65 | 2026-09-13 | RES-010, from GAP-010 | lib/{stamp.py,engine.py}, tests/{test_primitives.py,test_compiled.py,verify_engine.py,README.md}, ARCHITECTURE.md, ROADMAP.md, lib/__init__.py, .claude-plugin/plugin.json | **An artefact whose format carries no stamp is tracked in a sidecar, so `FOREIGN` can finally mean what it says.** `registry.base` is bare YAML and `START-HERE.md` carries no frontmatter, so both read `UNSTAMPED` on every run of every vault, including one written seconds earlier, and the branch whose docstring says `FOREIGN skip` fell through to `write`. `_system/os/stamps.json` holds one `source-sha`, `body-sha` and `generated-by` per such path; `stamp.record_for` and `stamp.verdict_recorded` answer the same four verdicts from it, and `engine._unstamped` is the branch. The state with no record splits by evidence rather than by assumption: a file hashing to what would be written is ours, the record is written down and nothing else happens, which is the whole of adoption and needs no flag; a file that differs is proof of nothing and is skipped, with `--force <id>` named in the row. The sidecar is a planned action, so it is backed up, listed in the apply manifest and undone by `--rollback` with everything else, and it carries no timestamp because a file recording when it was written could never be a NOOP. Comparing hashes rather than bytes finally reaches `strip_volatile` on the artefact it was written for: an apply no longer discards the column widths Obsidian puts in a `.base`. Eleven assertions, `ST-12` to `ST-16` and `CP-37` to `CP-42` including `CP-40b`, and the four archetypes' NOOP counts each rose by one, for the sidecar row. Each was shown red first by reverting what it guards, except `ST-13` and `CP-40b`, which pass on the reverted branch and are here for that reason: they hold the cases the fix must not change. 378/378. Version 0.32.0. | Found by the Counselor migration and written up as `GAP-010`, whose premise was itself incomplete. `FOREIGN` does not mean "a person wrote this", it means "no stamp", and the literal skip the card asks for turns `verify_engine` red on three checks because in a scenario vault those two files are ours and would be skipped forever. That is why the carrier had to come first and why the fix is not the branch. The carrier was chosen on what the format tolerates: a `#` comment inside the `.base` would be visible where it describes and would close item `A7` literally, and it stakes the most-opened artefact in the vault on Obsidian preserving a comment through a rewrite it demonstrably performs, while solving only one of the two files. `apply-manifest.json` was considered and rejected on inspection: it holds the PRE-image, is rewritten every apply, and carries no row at all for a file that did not change, which is the one case a provenance record is for. The column-width loss was not in the card until it was measured: injecting the block Obsidian writes into this vault’s own base turned its plan row from NOOP to `WRITE registry.base FOREIGN`. | done |
| 66 | 2026-09-13 | RES-011, from GAP-011 | runtime/_args.py (new), runtime/*.py (all nine), archetypes/*/manifest.toml, common/fragments/system/scripts-readme.md, tests/{test_args.py,run_tests.py,verify_engine.py,README.md}, ARCHITECTURE.md, ROADMAP.md, lib/__init__.py, .claude-plugin/plugin.json | **One argument reader for all nine scripts, refusing what it does not recognise.** The scripts validated no argument and answered no `--help`, in two different ways, because the rule was written nine times and drifted twice: two took `argv[1]` unconditionally, so `mint_change.py --help` created a directory named `--help` and claimed `CHG-001` inside it; seven filtered out anything starting with `--` and fell back to the working directory, so `gen_dashboard.py --check` discarded the flag and wrote the dashboard anyway and `validate_cards.py --help` reported `BLOCKER 0` over a folder it had never read. All three exited 0. `runtime/_args.py` is one pure function returning `(root, opts, done)` rather than calling `sys.exit`, because the refusal text is the product and a function that exits can only be tested through a captured stream. It answers `--help` and `-h` with exit 0, and refuses an undeclared option, a second path, or a value option given no value, each by name, with exit 2 and before any work. It ships as a tenth file in `_system/scripts/` in all four archetypes. Fourteen assertions in a new `test_args.py`: `AR-11` reads every shipped script and demands it routes through the reader, which is what stops a tenth script starting a third family, and `AR-14` runs every option each script accepted before and demands none is refused, because a refusal that also refuses what used to work is not a fix. `AR-01`, `02`, `06` and `07` are the positive path and hold the cases the fix must not change. Each of the rest was shown red by reverting what it guards, and two mutations were rewritten after the first attempt made them red by crashing rather than by failing, which is weaker evidence. 392/392, the four archetypes' NOOP counts each up one for the copied file. Version 0.33.0. | The defect was named as a trap in the handoff of 2026-09-12 and became a card only when it was measured. Measuring changed it twice. `gen_dashboard.py` does not treat the flag as data, which is how the trap was worded: it discards it and writes anyway, and the flag was the operator asking it not to. And the worst instance was one nobody had looked at, `validate_cards.py --help` answering `BLOCKER 0` to a question it did not understand, which the card still opened `medium` rather than `high` because the same line prints `cards 0` and this package will not report a coverage figure without its denominator. The second half of the fix is a sentence withdrawn rather than implemented. The seeded readme promised that nothing writes without `--write`; that cannot be true of a generator whose job is to write, and for `mint_change.py` it would reopen the read-then-write race that `O_CREAT | O_EXCL` was chosen to close and that three collisions in one vault proved. A promise nobody can keep is worse than a narrower one everybody can, so what is guaranteed now is only that an invocation the script does not understand is refused rather than performed. Because that fragment is `seeded`, the correction reaches new vaults only: every existing vault keeps a readme whose second half is false, and `RES-011` names that rather than hiding it. | done |
| 67 | 2026-09-13 | RES-012, from GAP-012 | lib/{engine.py,fsplan.py}, tests/{test_compiled.py,README.md}, ARCHITECTURE.md, ROADMAP.md, lib/__init__.py, .claude-plugin/plugin.json | **A copied file records the bytes it was copied from, so the package moving it is a different fact from the vault editing it.** The branch had three fates and needed six. It hashed the shipped source against the file and called every difference `LOCAL`, which is right for a file the vault edited and wrong for the ordinary case, the package changing it; with no record of what was last put there the two are one observation, resolved towards never writing. `engine._copied` now reads a record from the same `_system/os/stamps.json` `RES-010` introduced, holding the sha of the bytes last copied in. Absent copies and records. No record and identical adopts. No record and different is `FOREIGN`, skipped. Recorded and unedited with the source moved is **UPDATED**, which is the fate that did not exist and the reason a fix to a shipped script can be delivered at all. Recorded and edited is `LOCAL`, which is what the word always meant. `--force` takes the artefact id or the file’s own path, so taking eight scripts does not mean losing an edit to the ninth, and the nine identical rows collapse into one deferral saying what to type, because nine rows carrying the same paragraph is a paragraph nobody reads. Six assertions, `CP-43` to `CP-48`, each shown red by reverting what it guards; two test lines were made crash-proof first, because an assertion that dies on a `KeyError` proves the group is broken rather than that the claim is false. 398/398, plan-row counts unmoved. Version 0.34.0. | Found by asking where `RES-011` actually landed, four hours after releasing it, and the answer was nowhere. Nine scripts in the Solai vault were reported `differs from the shipped copy` on the day the fix shipped, and three were checked byte for byte against the previous release and matched exactly: nothing had been edited. `--from-package` was worse rather than better, installing `_args.py` beside nine scripts that do not import it. This is `GAP-010` one layer up, so it gets the same carrier and the same migration on purpose rather than a cleverer second answer: the same problem answered twice differently is how a codebase grows two families, which is what `GAP-011` was about. An ancestry of every sha ever shipped would have removed the one-time `--force` and was rejected on maintenance: a generated file whose staleness nothing detects, and a release step somebody must remember, in a package that had just spent a release removing a procedure that depends on somebody remembering. `R-10` had stood deferred since v0.1.0 on the reason "a vault that changed one has a hand edit", and nine counterexamples in one vault is the evidence a deferral names as its own reopening condition. | done |
