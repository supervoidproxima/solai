## What this vault is for

{remit}

First artefact this vault owes the world: **{first_artefact}**.

That line is not decoration. It is the denominator of every ratio `/solai` reports. A
vault that accumulates structure and never produces the named artefact is failing at the
only thing it was built for, and the tooling here is designed to say so out loud.
