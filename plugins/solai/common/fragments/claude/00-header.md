# CLAUDE.md - {name}

{remit}

This file is the vault's own instruction set. It inherits from `~/.claude/CLAUDE.md` and
from `~/.claude/vault-conventions.md`; where it differs, it says so and names the rule it
displaces. Sections marked as generated are rebuilt from the declarations in
`_system/os/` and must not be hand-edited: edit the declaration instead.
